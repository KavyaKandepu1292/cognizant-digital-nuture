server:
  port: 8082

spring:
  application:
    name: order-service
  datasource:
    url: jdbc:h2:mem:orderdb
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  # --- MySQL alternative (uncomment for real use) ---
  # datasource:
  #   url: jdbc:mysql://localhost:3306/orderdb
  #   username: root
  #   password: root
  #   driver-class-name: com.mysql.cj.jdbc.Driver

user-service:
  url: http://localhost:8081
