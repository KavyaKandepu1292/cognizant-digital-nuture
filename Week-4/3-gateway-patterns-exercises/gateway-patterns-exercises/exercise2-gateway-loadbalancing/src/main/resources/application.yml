server:
  port: 9081

spring:
  application:
    name: gateway-loadbalancing-service
  cloud:
    gateway:
      routes:
        - id: catalog_service_route
          uri: lb://catalog-service
          predicates:
            - Path=/balanced/**
