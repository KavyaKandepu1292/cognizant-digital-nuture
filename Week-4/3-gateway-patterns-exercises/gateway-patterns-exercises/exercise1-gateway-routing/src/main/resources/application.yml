server:
  port: 9080

spring:
  application:
    name: gateway-routing-service
  cloud:
    gateway:
      routes:
        - id: httpbin_route
          uri: https://httpbin.org
          predicates:
            - Path=/proxy/**
          filters:
            - RewritePath=/proxy/(?<segment>.*), /${segment}
