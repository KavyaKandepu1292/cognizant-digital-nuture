server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config-repo, file:./config-repo
        # --- git alternative (uncomment for real use) ---
        # git:
        #   uri: https://github.com/your-org/config-repo
        #   default-label: main
