// Document.java
// This is the BLUEPRINT — all document types must follow this

public interface Document {
    void open();    // every document must have open()
    void close();   // every document must have close()
}